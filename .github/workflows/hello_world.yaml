name: Hello World

on:
  push:
    branches:
      - main

jobs:
  hello-world-job:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Hello World
      - name: Hello World
        run: echo "Hello World GA Testing"

      # Step 2: Run Tests (includes setup, dependencies, and pytest)
      - name: Run Tests
        run: |
          echo "Setting up Python environment and running tests..."

          # Python setup (no separate step)
          python -m pip install --upgrade pip
          pip install pytest

          # Run tests
          pytest test_math.py
