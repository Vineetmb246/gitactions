name : Hello World
on:
  push:
     branches:
        -main 
jobs:
  first-hello-world-job :
    runs-on : ubuntu-latest
      steps :
          - name : Checkout Code
            uses : actions/checkout@v3
          - name : Print Messsage
            run : echo "Hello World GA Testing"
